\section{Self-Sustaining Equilibria}
\label{sec:wise}

\subsection{Productive value and the optimal composition fiber}
The active load set is fixed: wrench feasibility ($z\in X_{\mathrm f}$) guarantees each
load its physical service, and the productive value allocates \emph{total served
capacity} among the admitted loads through the \emph{served-capacity} aggregate $y=Bz$,
$y_k=\sum_{\tau,h}c_\tau z_{\tau kh}$ (distinct from the wrench aggregate $H_wz$; $y_k$ is
the productive reserve assigned to load $k$ beyond the wrench-feasibility floor---a proxy
for admissible throughput or disturbance tolerance):
\begin{equation}
V(z)=\phi(Bz),\qquad
\phi(y)=\sum_k\big(v_k\,y_k-\tfrac12\alpha\,y_k^2\big),
\label{eq:value}
\end{equation}
so $\phi(y)=-\tfrac12\|y-y_{\mathrm{ref}}\|_H^2+\mathrm{const}$ with $H=\alpha I\succ0$
and $y_{\mathrm{ref}}=v/\alpha$. It is strictly concave ($\nabla^2\phi=-\alpha I\prec0$)
and so pins down a unique optimal aggregate, whereas a deficit penalty such as
$-\|[d-s]_+\|^2$ would be flat on the feasible set and leave it undetermined. The game is
a constrained potential population game with payoff $\partial V/\partial z_{\tau a}$, and
identical-interest: the novelty lies in what $V$ leaves undetermined.

Wrench feasibility is a \emph{hard} shared constraint, its projected matrix $H_w$
(the wrench facets of Lemma~\ref{prop:membership}, $H_w z\ge d$) kept distinct from the
objective Hessian $H$:
\begin{equation}
X_{\mathrm f}=\{z\in\mathcal X:\;H_w z\ge d\}.
\label{eq:Xf}
\end{equation}
The action cost $g$ enters only as a selection refinement, \emph{excluded} from the
degeneracy result.

\begin{definition}[Productive variational equilibrium]\label{def:ve}
With $F(z)=-\nabla V(z)=-B^{\top}\nabla\phi(Bz)$, a profile $z^{\star}\in X_{\mathrm f}$ is a
\emph{productive variational equilibrium} if $\langle F(z^{\star}),z-z^{\star}\rangle\ge0$
for all $z\in X_{\mathrm f}$. As $V=\phi(Bz)$ is concave on the convex $X_{\mathrm f}$, this
holds iff $z^{\star}\in\arg\max_{z\in X_{\mathrm f}}V(z)$: the productive optimum \emph{is}
the variational equilibrium~\cite{facchinei2003fvi}, and its degeneracy is
Proposition~\ref{prop:fibermono}.
\end{definition}

\begin{theorem}[Composition degeneracy]\label{thm:degeneracy}
Since $\phi$ is strictly concave on the compact convex $BX_{\mathrm f}$
($H=\alpha I\succ0$), the productively optimal aggregate $y^{\star}=Bz^{\star}$ is unique;
but the optimal set $\Emanifold=\{z\in X_{\mathrm f}:Bz=y^{\star}\}$ is a nonempty compact
convex polytope whose local dimension at a relative-interior point $\bar z$ is
\begin{equation}
\dim\Emanifold=\dim\!\big(\ker A\cap\ker B\cap\ker G_I\big)
=n-\operatorname{rank}[A;B;G_I],
\label{eq:dim}
\end{equation}
where $Az=b$ is budget conservation, $Bz=y^{\star}$ the served aggregate, $G_Iz=h_I$
the inequalities of $X_{\mathrm f}$ active at $\bar z$ (nonnegativity, occupancy, active
wrench), and $n=\dim z$. Thus $\Emanifold$ is positive-dimensional iff the
\emph{productive neutral space} $N_p(\bar z)=\ker A\cap\ker B\cap\ker G_I$ is nontrivial.
\end{theorem}
\begin{proof}
Uniqueness of $y^{\star}$ is strict concavity of $\phi$. At
$\bar z\in\operatorname{relint}\Emanifold$ the minimal face $F$ fixes exactly the
active constraints, so the feasible perturbations are
$\{d:Ad=0,\,Bd=0,\,G_Id=0\}=N_p(\bar z)$; hence $\Emanifold$ locally coincides with
$\bar z+N_p(\bar z)$ and \eqref{eq:dim} is rank--nullity. Since $V=\phi(Bz)$ depends on
$z$ only through $Bz$, every $z\in\Emanifold$ attains the same $V$; a general weighted
$B$ can give $N_p=\{0\}$ (a single composition).
\end{proof}
For the representative instance ($n=72$), $\operatorname{rank}[A;B]=13$ and no inequality
is active at the interior optimum ($G_I=\varnothing$), so the fiber is full-dimensional in
its aggregate-fixing affine space: $\dim\Emanifold=72-13=59$, certified by a
relative-interior point with neutral-direction residuals
$\|Ad\|,\|Bd\|,\|G_Id\|<10^{-15}$ (dimensions computed in $z$ with the projected wrench
facets $H_w$; \texttt{exp\_fiber}). At a boundary optimum where wrench facets bind, the
same formula gives a smaller fiber.

\begin{proposition}[Fiber--monotonicity equivalence]\label{prop:fibermono}
On the active-face tangent $\mathcal T_F=\ker A\cap\ker G_I$ the pseudo-gradient
$F(z)=B^{\top}H(Bz-y_{\mathrm{ref}})$ obeys $\langle F(z{+}d)-F(z),d\rangle=\|H^{1/2}Bd\|^2$,
so its strong-monotonicity modulus is $\mu_F=\sigma_{\min}^2(H^{1/2}B|_{\mathcal T_F})$ with
$\ker(B|_{\mathcal T_F})=N_p(\bar z)$. Hence $\dim\Emanifold>0\iff\mu_F=0$: strong
monotonicity holds in the served aggregate ($H\succ0$) but is \emph{lost} on the
composition face---the degeneracy WISE exploits, unlike strongly contractive aggregative
games~\cite{martinezpiazuelo2022}.
\end{proposition}

A comparative-advantage exchange makes this concrete (Fig.~\ref{fig:central}b,c): with two
types of equal lifting capacity $c_\ell=c_s$, swapping which type lifts and which relays is
productively neutral ($Bd=0$, $V\equiv V^{\star}$) with spectral effect
$D\lamtwo[d]=\sum_{\tau,r}\gamma_{\tau r}d_{\tau r}\,v^{\top}L_r v$. Whether such a
\emph{free} gain exists is a projection condition:

\begin{proposition}[Free-connectivity criterion]\label{prop:freeconn}
Let $\bar z\in\operatorname{relint}\Emanifold$ with $\lamtwo$ simple and Fiedler vector $v$,
so the locally feasible directions form the subspace $N_p=\ker A\cap\ker B\cap\ker G_I$ (no
constraint beyond those defining the minimal active face becomes newly active). With
$g_\lambda=\nabla_z\lamtwo(\Lap(\bar z))$ (entries $\gamma_{\tau r}v^{\top}L_r v$),
\begin{equation}
\max_{h\in N_p,\ \|h\|\le1}D\lamtwo(\bar z)[h]=\|\Pi_{N_p}g_\lambda\|,
\label{eq:freeconn}
\end{equation}
so a productively neutral improvement exists $\iff\Pi_{N_p}g_\lambda\neq0$, with optimal
direction $h^\star=\Pi_{N_p}g_\lambda/\|\Pi_{N_p}g_\lambda\|$. At a boundary $\bar z$ replace
$N_p$ by the tangent cone $T_{\Emanifold}(\bar z)=\{h:Ah{=}0,\ Bh{=}0,\ G_jh\le0,\ j\in
I(\bar z)\}$ ($I(\bar z)$ the active set, sign convention $Gz\le h$); then no free
improvement exists $\iff g_\lambda\in N_{\Emanifold}(\bar z)=\{A^{\top}\alpha+B^{\top}\beta+
G_I^{\top}\nu:\nu\ge0\}$, the KKT-stationarity certificate. Under a repeated $\lamtwo$,
replace $D\lamtwo[h]$ by $\lambda_{\min}(U_2^{\top}D\Lap(\bar z)[h]\,U_2)$, where $U_2$ is an
orthonormal basis of the eigenspace of the \emph{smallest} eigenvalue of
$Q^{\top}\Lap(\bar z)Q$; a first-order neutral improvement then exists
$\iff\max_{h\in T_{\Emanifold},\|h\|\le1}\lambda_{\min}(U_2^{\top}D\Lap[h]U_2)>0$ (a small
SDP).
\end{proposition}

\subsection{The WISE refinement}
\begin{definition}[WISE and its integer counterparts]\label{def:wise}
A profile $z^{\star}\in\Emanifold$ is a \emph{Wrench- and
Information-Self-Sustaining Equilibrium} if $\lamtwo(\Lap(z^{\star}))\ge\sigmastar$, i.e.\
$z^{\star}\in\Essrel:=\Emanifold\cap\Cdelta$ over the fluid relaxation, where
$\Cdelta=\{z\in X_{\mathrm f}:\lamtwo(\Lap(z))\ge\sigmastar\}$. For finite teams three
integer notions differ: the \emph{self-sustaining feasible} set
$\mathcal F_{\mathbb Z}=\{z\in X_{\mathrm f}\cap\mathbb Z^{n}:\lamtwo(\Lap(z))\ge\sigmastar\}$
(wrench- and connectivity-feasible), its \emph{optimum}
$V_{\mathbb Z}^{\star}=\max_{\mathcal F_{\mathbb Z}}V$, and the \emph{zero-cost integer WISE}
$\Essint=\mathcal F_{\mathbb Z}\cap\{Bz=y^{\star}\}$, which is nonempty iff
$V_{\mathbb Z}^{\star}=V^{\star}$; the productive integrality gap is
$g_{\mathbb Z}=V^{\star}-V_{\mathbb Z}^{\star}\ge0$. Since
$\sigmastar=\sigmadyn+\delta>\sigmadyn$, a WISE clears the stability threshold of
Remark~\ref{thm:stability} with margin $\delta$ and, by Lemma~\ref{lem:bridge},
keeps the \emph{geometric} graph above $\sigmadyn$. $\Cdelta$ is convex and closed
(super-level set of the concave $\lamtwo\!\circ\!\Lap$, Lemma~\ref{lem:convex}). WISE is a
\emph{lexicographic selection} from the productive VE set, not an equilibrium generated by
the productive game alone.
\end{definition}

\begin{theorem}[Relaxed WISE existence and selection as an SDP]\label{thm:existence}
Over the fluid relaxation, \emph{Stage 1} fixes the unique optimal aggregate
$y^{\star}=Bz^{\star}$, $z^{\star}\in\arg\max_{z\in X_{\mathrm f}}V(z)$
(Theorem~\ref{thm:degeneracy}); \emph{Stage 2} selects connectivity on the resulting
fiber. The lexicographic selector $z^{\star}_{\mathrm{WISE}}\in
\arg\max_{z\in\Emanifold}\lamtwo(\Lap(z))$ is the solution of the Stage-2 semidefinite
program, stated in the \emph{lifted} wrench variables of Lemma~\ref{prop:membership}
(so no facet enumeration of $X_{\mathrm f}$ is needed),
\begin{equation}
\LamE=\max_{z,\,\xi,\,t}\ t\quad\text{s.t.}\quad
z\in\mathcal X,\ Bz=y^{\star},\ \eqref{eq:tensor},\ Q^{\top}\Lap(z)\,Q\succeq tI,
\label{eq:sdp}
\end{equation}
convex by Lemma~\ref{lem:convex} (affine constraints in $(z,\xi)$, concave
$\lamtwo\!\circ\!\Lap$). Over the relaxation a \emph{relaxed} WISE exists iff the value
clears the requirement,
\begin{equation}
\Essrel\neq\varnothing\iff\LamE\ge\sigmastar,
\label{eq:exist}
\end{equation}
and any maximiser $z^{\star}_{\mathrm{WISE}}$ preserves the productive optimum exactly,
$Bz^{\star}_{\mathrm{WISE}}=y^{\star}$, hence is itself a variational equilibrium
(Def.~\ref{def:ve}) with $V=V^{\star}$. When
$\min_{\Emanifold}\lamtwo<\sigmastar\le\LamE$ the refinement is \emph{strict}. The
argmax set $\Emanifold^{\star}$ may be non-singleton; the strongly convex tie-break
$\min_{z\in\Emanifold^{\star}}\tfrac12\|z-z_{\mathrm{prev}}\|_W^2$ ($W\succ0$, least role
change from the previous assignment) fixes a \emph{unique} selector, which a linear
$g^{\top}z$ need not.
\end{theorem}
\begin{proof}
$X_{\mathrm f}$ is a convex polytope and $Bz=y^{\star}$ is affine; by Lemma~\ref{lem:convex},
$\{(z,t):Q^{\top}\Lap(z)Q\succeq tI\}$ is convex, so \eqref{eq:sdp} is an SDP; its value
$\LamE=\max_{z\in\Emanifold}\lamtwo(\Lap(z))$ is attained on the compact $\Emanifold$
(Weierstrass). $\Essrel=\Emanifold\cap\Cdelta$ is nonempty iff this maximum reaches
$\sigmastar$, giving \eqref{eq:exist}; feasibility forces
$Bz^{\star}_{\mathrm{WISE}}=y^{\star}$. Strictness holds when $\sigmastar$ lies
strictly inside the range of $\lamtwo$ over $\Emanifold$.
\end{proof}

\subsection{Aggregate--estimator stability (motivating)}
\begin{remark}[Connectivity threshold]\label{thm:stability}
A standard small-gain model---a served-aggregate error coupled to a consensus estimator over
$Q^{\top}\Lap(z^{\star})Q$ with gains $\mF,\vt_1,\vt_2>0$---is exponentially stable when
$c\,\mF\,\lamtwo(\Lap(z^{\star}))>\vt_1\vt_2$, i.e.\ $\lamtwo>\sigmadyn:=\vt_1\vt_2/(c\,\mF)$
(single-mode necessary). It motivates treating $\sigmadyn>0$ as an external information-layer
requirement; no online optimizer--estimator realization is analyzed here, and the WISE margin
$\sigmastar=\sigmadyn+\delta$ satisfies it by construction.
\end{remark}

\subsection{The price of self-sustainability}
\label{sec:price}
The lexicographic selector of Theorem~\ref{thm:existence} is exact; a scalarised
$V_\varepsilon=V+\varepsilon\lamtwo$ recovers it only as $\varepsilon\to0$: any maximiser
obeys $0\le V^{\star}-V(z_\varepsilon)\le\varepsilon\operatorname{osc}(\lamtwo)$ and
$\|Bz_\varepsilon-y^{\star}\|\le\sqrt{2\varepsilon\operatorname{osc}(\lamtwo)/\alpha}$
(Tikhonov~\cite{facchinei2003fvi}), so a finite $\varepsilon$ moves the aggregate
(measurably, Section~\ref{sec:exp}); it is a Pareto scalarisation, not an equivalent of the
lexicographic form.

Beyond the free fiber the requirement carries a productive cost. Define the
\emph{price of self-sustainability}
\begin{equation}
\Price(\sigma)=V^{\star}-\max\{V(z):z\in X_{\mathrm f},\ \lamtwo(\Lap(z))\ge\sigma\}
\label{eq:price}
\end{equation}
($\Price(\sigma)=+\infty$ when the set is empty), and the two spectral capacities
$\LamE=\max_{z\in\Emanifold}\lamtwo(\Lap(z))$ and
$\LamX=\max_{z\in X_{\mathrm f}}\lamtwo(\Lap(z))\ge\LamE$.

\begin{theorem}[Price and the free/costly/impossible trichotomy]\label{thm:price}
$\Price$ is a nondecreasing, proper extended-value convex function, and
\begin{equation}
\Price(\sigma)=
\begin{cases}
0, & \sigma\le\LamE\ \ (\text{free}),\\[1pt]
\text{finite}>0, & \LamE<\sigma\le\LamX\ \ (\text{costly}),\\[1pt]
+\infty, & \sigma>\LamX\ \ (\text{impossible}).
\end{cases}
\label{eq:trichotomy}
\end{equation}
At any $\sigma<\LamX$ admitting a strictly feasible point ($\exists z\in X_{\mathrm f}$ with
$\lamtwo(\Lap(z))>\sigma$, i.e.\ Slater holds), the optimal multiplier $\pi^{\star}$ of the
connectivity constraint satisfies $\pi^{\star}\in\partial\Price(\sigma)$: the marginal
productive price of one unit of connectivity, in the units of $V$.
\end{theorem}
\begin{proof}
The set $\{z\in X_{\mathrm f}:\lamtwo(\Lap(z))\ge\sigma\}$ is a super-level set of the
concave $\lamtwo\!\circ\!\Lap$ (Lemma~\ref{lem:convex}), hence convex and shrinking in
$\sigma$; so the constrained maximum is nonincreasing and $\Price$ nondecreasing. As
$\sigma$ enters the single concave inequality $\lamtwo\ge\sigma$ linearly, $\Price$ is the
perturbation value of a convex program, so it is convex and, under Slater, its
subdifferential is the set of optimal multipliers $\pi^{\star}$ of that
constraint~\cite{boyd2004convex}. For $\sigma\le\LamE$ some $z\in\Emanifold$ attains
$\lamtwo\ge\sigma$ with $V=V^{\star}$, so $\Price=0$; for $\sigma>\LamX$ no wrench-feasible
$z$ reaches $\sigma$ and $\Price=+\infty$; for $\LamE<\sigma\le\LamX$ a feasible $z$ exists
but none in $\Emanifold$, so $0<\Price<\infty$.
\end{proof}
This makes the trichotomy a certificate (Section~\ref{sec:exp}: $\Price\equiv0$ up to
$\LamE$, a strictly positive branch on $(\LamE,\LamX]$, then infeasibility), with
$\pi^{\star}$ pricing connectivity in the same units as $V$.

\subsection{Finite-team recovery}
A sufficient margin test preserves \emph{joint feasibility} after integer recovery (it does
not by itself preserve the aggregate). Let $z^{\star}$ be a relaxed WISE and define the
wrench and connectivity slacks and the sensitivity constants
\begin{equation}
\begin{aligned}
&m_w=\min_j\big((H_wz^{\star})_j-d_j\big),\quad
m_\lambda=\lamtwo(\Lap(z^{\star}))-\sigmastar,\\
&\kappa_w=\|H_w\|_{2\to\infty},\qquad
\kappa_L=\sup_{\|d\|=1}\|D\Lap[d]\|_2 .
\end{aligned}
\label{eq:margins}
\end{equation}
($H_wz\ge d$ is formed offline only for these margins; the SDP stays lifted.)
\begin{proposition}[Margin-based preservation of joint feasibility]\label{prop:integer}
Let $\hat z$ be any integer assignment meeting budgets, integrality and occupancy exactly
with $\|\hat z-z^{\star}\|_2\le r$. If
\begin{equation}
r<\min\!\Big\{\tfrac{m_w}{\kappa_w},\ \tfrac{m_\lambda}{\kappa_L}\Big\},
\label{eq:intmargin}
\end{equation}
then $\hat z\in X_{\mathrm f}$ and $\lamtwo(\Lap(\hat z))\ge\sigmastar$: $\hat z$ is an integer
self-sustaining assignment. If additionally $B\hat z=y^{\star}$ it is a zero-cost integer
WISE; otherwise its productive loss $g_{\mathbb Z}$ is evaluated separately
(Section~\ref{sec:exp}).
\end{proposition}
\begin{proof}
Row-wise, $(H_w\hat z)_j\ge(H_wz^{\star})_j-\|(H_w)_j\|_2\,r\ge d_j+m_w-\kappa_w r>d_j$ by
$r<m_w/\kappa_w$, so $\hat z\in X_{\mathrm f}$. As $\Lap$ is affine in $z$,
$\|\Lap(\hat z)-\Lap(z^{\star})\|_2\le\kappa_L r$, and Weyl's inequality on
$Q^{\top}\Lap Q$ gives
$\lamtwo(\Lap(\hat z))\ge\lamtwo(\Lap(z^{\star}))-\kappa_L r=\sigmastar+m_\lambda-\kappa_L r
>\sigmastar$ by $r<m_\lambda/\kappa_L$.
\end{proof}

\begin{remark}[Rounding as a search]\label{rem:integer}
Proposition~\ref{prop:integer} is a checkable sufficient condition, not an existence
guarantee: it does not assert an integer $\hat z$ within radius $r$ exists. Best-of-$R$
rounding \emph{searches} for one and re-certifies it against~\eqref{eq:margins} (a search,
not a proof; no $O(1/N)$ claim). On the $N=12$ demonstration a single randomized-rounding
draw re-certifies a fluid optimum with probability $0.40$ and best-of-$30$ re-certifies
$29/30$ seeds (argmax $7/30$); an exact integer oracle for $N\le8$ finds $0\%$ false
positives and integrality gap $\le0.002$ (Section~\ref{sec:exp}). The radius
\eqref{eq:intmargin} is sufficient but conservative (median $r^\star{\approx}0.006$,
$\chi{=}\|\hat z-z^\star\|/r^\star{\approx}190$ despite slacks $m_w{=}3.6$,
$m_\lambda{=}0.30$), so direct re-certification is the operative test.
\end{remark}