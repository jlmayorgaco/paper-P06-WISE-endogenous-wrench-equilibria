\section{Self-Sustaining Equilibria}
\label{sec:wise}

\subsection{Productive value and the optimal composition fiber}
The active load set is fixed: wrench feasibility ($z\in X_{\mathrm f}$) guarantees each
load its minimum physical service, and the productive value allocates \emph{reserve}
capacity---served capacity beyond that minimum---among the admitted loads through the
\emph{served-capacity} aggregate $y=Bz$, $y_k=\sum_{\tau,h}c_\tau z_{\tau kh}$:
\begin{equation}
V(z)=\phi(Bz),\qquad
\phi(y)=\sum_k\big(v_k\,y_k-\tfrac12\alpha\,y_k^2\big),
\label{eq:value}
\end{equation}
so $\phi(y)=-\tfrac12\|y-y_{\mathrm{ref}}\|_H^2+\mathrm{const}$ with $H=\alpha
I\succ0$ and $y_{\mathrm{ref}}=v/\alpha$. It is strictly concave
($\nabla^2\phi=-\alpha I\prec0$) and so pins down a unique optimal aggregate; a
deficit penalty such as $-\|[d-s]_+\|^2$ would be flat on the feasible set and
leave the aggregate undetermined. The game is a constrained potential population
game with payoff $\partial V/\partial z_{\tau a}$, and it is identical-interest:
the novelty lies in what $V$ leaves undetermined.

Wrench feasibility is a \emph{hard} shared constraint, its projected matrix $H_w$
(the wrench facets of Lemma~\ref{prop:membership}, $H_w z\ge d$) kept notationally
distinct from the objective Hessian $H$:
\begin{equation}
X_{\mathrm f}=\{z\in\mathcal X:\;H_w z\ge d\}.
\label{eq:Xf}
\end{equation}
The action cost $g$ enters only as a selection refinement and is \emph{excluded}
from the degeneracy result.

\begin{definition}[Productive variational equilibrium]\label{def:ve}
With pseudo-gradient $F(z)=-\nabla V(z)=-B^{\top}\nabla\phi(Bz)$, a profile
$z^{\star}\in X_{\mathrm f}$ is a \emph{productive variational equilibrium} if
\begin{equation}
\langle F(z^{\star}),\,z-z^{\star}\rangle\ge0\quad\forall z\in X_{\mathrm f},
\label{eq:vi}
\end{equation}
i.e.\ $z^{\star}$ solves $\mathrm{VI}(X_{\mathrm f},F)$. Since $V=\phi(Bz)$ is concave
on the convex $X_{\mathrm f}$, \eqref{eq:vi} holds iff
$z^{\star}\in\arg\max_{z\in X_{\mathrm f}}V(z)$: the productive optimum \emph{is} the
variational equilibrium~\cite{facchinei2003fvi}. Its composition degeneracy---and its
precise link to the loss of strong monotonicity---is Proposition~\ref{prop:fibermono}.
\end{definition}

\begin{lemma}[Unique productive aggregate]\label{lem:uniqueagg}
If $\phi$ is strictly concave on $BX_{\mathrm f}$ ($H=\alpha I\succ0$) and
$X_{\mathrm f}$ is nonempty compact convex, the productively optimal aggregate
$y^{\star}=Bz^{\star}$ is unique. Uniqueness is claimed for the \emph{aggregate},
not the composition.
\end{lemma}
\begin{proof}
$\phi$ strictly concave on the compact convex $BX_{\mathrm f}$ attains its maximum
at a unique $y^{\star}$: two distinct optima would have a strictly better midpoint.
\end{proof}

\begin{theorem}[Composition degeneracy]\label{thm:degeneracy}
Under Lemma~\ref{lem:uniqueagg} the aggregate $y^{\star}=Bz^{\star}$ is unique, but
the optimal set $\Emanifold=\{z\in X_{\mathrm f}:Bz=y^{\star}\}$ is a nonempty compact
convex polytope whose local dimension at a relative-interior point $\bar z$ is
\begin{equation}
\dim\Emanifold=\dim\!\big(\ker A\cap\ker B\cap\ker G_I\big)
=n-\operatorname{rank}[A;B;G_I],
\label{eq:dim}
\end{equation}
where $Az=b$ is budget conservation, $Bz=y^{\star}$ the served aggregate, $G_Iz=h_I$
the inequalities of $X_{\mathrm f}$ active at $\bar z$ (nonnegativity, occupancy, active
wrench), and $n=\dim z$. Thus $\Emanifold$ is positive-dimensional iff the
\emph{productive neutral space} $N_p(\bar z)=\ker A\cap\ker B\cap\ker G_I$ is nontrivial;
the transportation value $(T-1)(K_{\mathrm{act}}-1)$ is the special case of equal
column-sum weights.
\end{theorem}
\begin{proof}
Uniqueness of $y^{\star}$ is Lemma~\ref{lem:uniqueagg}. At
$\bar z\in\operatorname{relint}\Emanifold$ the minimal face $F$ fixes exactly the
active constraints, so the feasible perturbations are
$\{d:Ad=0,\,Bd=0,\,G_Id=0\}=N_p(\bar z)$; hence $\Emanifold$ locally coincides with
$\bar z+N_p(\bar z)$ and \eqref{eq:dim} is rank--nullity. Since $V=\phi(Bz)$ depends on
$z$ only through $Bz$, every $z\in\Emanifold$ attains the same $V$; a general weighted
$B$ can give $N_p=\{0\}$ (a single composition).
\end{proof}
For the representative instance we obtain $\dim\Emanifold=4$ out of $n=72$, with the
neutral-direction residuals $\|Ad\|,\|Bd\|,\|G_Id\|$ at machine precision.

\begin{proposition}[Fiber--monotonicity equivalence]\label{prop:fibermono}
On the active-face tangent $\mathcal T_F=\ker A\cap\ker G_I$, the pseudo-gradient
$F(z)=B^{\top}H(Bz-y_{\mathrm{ref}})$ obeys
$\langle F(z{+}d)-F(z),d\rangle=\|H^{1/2}Bd\|^2$ for $d\in\mathcal T_F$, so its
strong-monotonicity modulus there is
$\mu_F=\sigma_{\min}^2(H^{1/2}B|_{\mathcal T_F})$ with
$\ker(B|_{\mathcal T_F})=N_p(\bar z)$. Hence $\dim\Emanifold>0\iff\mu_F=0$: $F$ is
strongly monotone on $\mathcal T_F$ exactly when the fiber is a point. Strong
monotonicity survives in the served aggregate ($H\succ0$) and in the quotient
$\mathcal T_F/N_p$, but is \emph{lost} on the full composition face---this is precisely
the degeneracy WISE exploits, in contrast to strongly contractive aggregative
games~\cite{martinezpiazuelo2022}.
\end{proposition}

\begin{example}[A two-type fiber]\label{ex:fiber}
With $T=2$ types of \emph{equal lifting capacity} $c_\ell=c_s$ but only the long-range
type relaying effectively ($\gamma_{\ell r}>\gamma_{s r}$), moving $\alpha$ long-range
robots from lifting to relay and an equal mass of short-range robots the other way is a
productively neutral swap ($Bd=0$): $Bz(\alpha)=y^{\star}$, $V\equiv V^{\star}$, while
$\lamtwo$ rises by the comparative-advantage derivative
$D\lamtwo[d]=\sum_{\tau,r}\gamma_{\tau r}d_{\tau r}\,v^{\top}L_r v>0$ (simple $v$). The
SDP takes the largest feasible $\alpha$; a WISE exists iff it clears $\sigmastar$
(Fig.~\ref{fig:scene}, block~2).
\end{example}

\subsection{The WISE refinement}
Place the endogenous Laplacian \eqref{eq:laplacian} on $\Emanifold$.

\begin{definition}[WISE]\label{def:wise}
A profile $z^{\star}\in\Emanifold$ is a \emph{Wrench- and
Information-Self-Sustaining Equilibrium} if $\lamtwo(\Lap(z^{\star}))\ge\sigmastar$,
i.e.\ $z^{\star}\in\Ess:=\Emanifold\cap\Cdelta$, where
$\Cdelta=\{z\in X_{\mathrm f}:\lamtwo(\Lap(z))\ge\sigmastar\}$. Since
$\sigmastar=\sigmadyn+\delta>\sigmadyn$, a WISE clears the stability threshold of
Theorem~\ref{thm:stability} with margin $\delta$ and, by Lemma~\ref{lem:bridge},
keeps the \emph{geometric} graph above $\sigmadyn$. $\Cdelta$ is convex and closed
(super-level set of the concave $\lamtwo\!\circ\!\Lap$, Lemma~\ref{lem:convex}).
\end{definition}

\begin{theorem}[WISE existence and selection as an SDP]\label{thm:existence}
\emph{Stage 1} fixes the unique optimal aggregate
$y^{\star}=Bz^{\star}$, $z^{\star}\in\arg\max_{z\in X_{\mathrm f}}V(z)$
(Lemma~\ref{lem:uniqueagg}); \emph{Stage 2} selects connectivity on the resulting
fiber. The lexicographic selector $z^{\star}_{\mathrm{WISE}}\in
\arg\max_{z\in\Emanifold}\lamtwo(\Lap(z))$ is the solution of the Stage-2 semidefinite
program, stated in the \emph{lifted} wrench variables of Lemma~\ref{prop:membership}
(so no facet enumeration of $X_{\mathrm f}$ is needed),
\begin{equation}
\LamE=\max_{z,\,\xi,\,t}\ t\quad\text{s.t.}\quad
z\in\mathcal X,\ Bz=y^{\star},\ \eqref{eq:tensor},\ Q^{\top}\Lap(z)\,Q\succeq tI,
\label{eq:sdp}
\end{equation}
convex by Lemma~\ref{lem:convex} (affine constraints in $(z,\xi)$, concave
$\lamtwo\!\circ\!\Lap$).
Since $\lambda_{\min}(A)=\min_{Z\succeq0,\operatorname{tr}Z=1}\langle Z,A\rangle$, it is
the \emph{spectral saddle}
\begin{equation}
\LamE=\max_{z\in\Emanifold}\ \min_{Z\succeq0,\,\operatorname{tr}Z=1}
\big\langle Z,\,Q^{\top}\Lap(z)Q\big\rangle,
\label{eq:saddle}
\end{equation}
with $\max$ and $\min$ interchangeable (Sion): the populations pick a productively
equivalent composition while a spectral adversary $Z$ picks the worst disagreement
mode. A WISE exists iff the value clears the requirement,
\begin{equation}
\Ess\neq\varnothing\iff\LamE\ge\sigmastar,
\label{eq:exist}
\end{equation}
and any maximiser $z^{\star}_{\mathrm{WISE}}$ preserves the productive optimum exactly,
$Bz^{\star}_{\mathrm{WISE}}=y^{\star}$, hence is itself a variational equilibrium
(Def.~\ref{def:ve}) with $V=V^{\star}$. When
$\min_{\Emanifold}\lamtwo<\sigmastar\le\LamE$ the refinement is \emph{strict}. The
argmax set $\Emanifold^{\star}$ may be non-singleton; a lexicographic tie-break
$\min_{z\in\Emanifold^{\star}}g^{\top}z$ (least role change) fixes a unique selector.
\end{theorem}
\begin{proof}
$X_{\mathrm f}$ and $Bz=y^{\star}$ are affine and, by Lemma~\ref{lem:convex},
$\{(z,t):Q^{\top}\Lap(z)Q\succeq tI\}$ is convex, so \eqref{eq:sdp} is an SDP; its value
$\LamE=\max_{z\in\Emanifold}\lamtwo(\Lap(z))$ is attained on the compact $\Emanifold$
(Weierstrass). $\Ess=\Emanifold\cap\Cdelta$ is nonempty iff this maximum reaches
$\sigmastar$, giving \eqref{eq:exist}; feasibility forces
$Bz^{\star}_{\mathrm{WISE}}=y^{\star}$. Strictness holds when $\sigmastar$ lies
strictly inside the range of $\lamtwo$ over $\Emanifold$.
\end{proof}

\subsection{Aggregate--estimator stability}
The decentralized realization~\eqref{eq:pd} couples a served-aggregate revision error
$\xi$ with a consensus-estimator error $\eta$. To establish the \emph{dynamic
significance} of the spectral threshold---not to claim a full linearisation
of~\eqref{eq:pd}---we posit a local optimizer--estimator interconnection model with
possibly distinct couplings,
\begin{equation}
\dot\xi=-M\xi+\Theta_1\eta,\qquad
\dot\eta=\Theta_2\,\xi-c\,(\bar L_\perp\!\otimes\! I_p)\,\eta,
\label{eq:jac}
\end{equation}
$\bar L_\perp=Q^{\top}\Lap(z^{\star})Q$, under the hypotheses $\tfrac12(M+M^{\top})\succeq
\mF I$ (optimizer curvature), $\|\Theta_1\|_2\le\vt_1$, $\|\Theta_2\|_2\le\vt_2$.

\begin{theorem}[Optimizer--estimator small gain]\label{thm:stability}
Under those hypotheses, \eqref{eq:jac} is exponentially stable whenever
\begin{equation}
c\,\mF\,\lamtwo(\Lap(z^{\star}))>\vt_1\vt_2,
\quad\text{i.e.}\quad
\lamtwo>\sigmadyn:=\frac{\vt_1\vt_2}{c\,\mF}.
\label{eq:threshold}
\end{equation}
The reciprocal isotropic case $\Theta_2=\Theta_1^{\top}$, $\vt_1=\vt_2=\vt$ recovers
$c\,\mF\,\lamtwo>\vt^2$, which is then also \emph{necessary} on the Fiedler mode.
\end{theorem}
\begin{proof}
Take $W=\tfrac12\|\xi\|^2+\tfrac{\vt_1}{2\vt_2}\|\eta\|^2$. Then
$\dot W\le-\mF\|\xi\|^2-\tfrac{\vt_1}{\vt_2}c\lamtwo\|\eta\|^2+2\vt_1\|\xi\|\|\eta\|$,
a negative-definite quadratic form iff $\mF\cdot\tfrac{\vt_1}{\vt_2}c\lamtwo>\vt_1^2$,
i.e.\ \eqref{eq:threshold}; the isotropic symmetric case is tight on the Fiedler mode.
\end{proof}
This is a conditional interconnection result, \emph{not} a convergence proof
for~\eqref{eq:pd}: WISE existence and selection rest on the exact SDP. It links the
connectivity requirement to a local stability boundary, which WISE's strict margin
$\sigmastar=\sigmadyn+\delta$ clears with room.

\subsection{Selection, prices, and the cost of endogeneity}
The lexicographic selector of Theorem~\ref{thm:existence} is exact. A scalarised
alternative $V_\varepsilon=V+\varepsilon\lamtwo$ recovers it only in the limit: any
maximiser obeys $0\le V^{\star}-V(z_\varepsilon)\le\varepsilon\operatorname{osc}(\lamtwo)$
and $\|Bz_\varepsilon-y^{\star}\|\le\sqrt{2\varepsilon\operatorname{osc}(\lamtwo)/\alpha}$
(Tikhonov~\cite{facchinei2003fvi}), so a finite $\varepsilon$ perturbs the aggregate
whereas the lexicographic form does not. The Lagrangian of \eqref{eq:sdp} carries duals
$\mu\ge0$ (wrench), $\nu$ (aggregate), $\rho$ (budget/occupancy), and PSD $Z\succeq0$
(spectral); stationarity
$H_w^{\top}\mu-B^{\top}\nu-A^{\top}\rho+\mathcal G^{*}(Z)=0$ prices wrench feasibility
through $\mu$ and certifies connectivity through $Z$, conflating no multiplier with a
marginal utility.

\begin{corollary}[Free / costly / impossible]\label{thm:trichotomy}
With $\LamE$ from \eqref{eq:sdp} and $\LamX=\max_{z\in X_{\mathrm f}}\lamtwo(\Lap(z))$
(both attained by Weierstrass), the endogeneity cost
$\Ddelta=V^{\star}-\max_{z\in X_{\mathrm f}\cap\Cdelta}V(z)$ partitions by $\sigmastar$
relative to $\LamE\le\LamX$: \emph{(free)} $\LamE\ge\sigmastar$ gives
$\Ess\neq\varnothing$, $\Ddelta=0$; \emph{(costly)} $\LamE<\sigmastar\le\LamX$ has no
safe optimal composition but a safe feasible one, $\Ddelta>0$ (measured against
$X_{\mathrm f}\cap\Cdelta$, not $\Ess$ where $V\equiv V^{\star}$); \emph{(impossible)}
$\LamX<\sigmastar$ gives $X_{\mathrm f}\cap\Cdelta=\varnothing$, $\Ddelta=+\infty$.
\end{corollary}

\begin{remark}[Finite-population recovery]\label{rem:integer}
The analysis is a fluid relaxation; integer assignments are recovered by rounding and
re-certified numerically (no $O(1/N)$ claim). The robustness margins $s\ge d+m_W$ and
$\lamtwo\ge\sigmastar$ leave slack, so on the $N=12$ demonstration the fluid
certification rate carries over to integer robots ($100\%$ under randomised rounding),
whereas naive rounding of a zero-margin optimum does not.
\end{remark}