\section{Simulation Study}
\label{sec:exp}

We validate the main results on heterogeneous teams ($N=12$ unless noted) in a two-region
world: a rigid load demands a planar wrench, short links connect robots only within a
cluster, and the gap is bridged only when a long-range type relays. The lifters are
second-order unicycles and the load a planar rigid body,
\begin{equation}
\dot p_i=v_i[\cos\theta_i,\sin\theta_i]^{\!\top},\;\;
m_i\dot v_i=u_{v,i}-d_iv_i,\;\;J_i\dot\omega_i=u_{\omega,i}-d_{\omega i}\omega_i,
\label{eq:unicycle}
\end{equation}
with load wrench $\sum_{k,h}A_{kh}f_{i}$ from the admissible contact forces $f_i\in
U_\tau(\theta_i)$. The primary metric is the \emph{certified service rate}: served only
when wrench-feasible \emph{and} $\lamtwo-\sigmastar\ge0$. Every instance is seeded and
reproduces exactly.

\begin{figure*}[t]\centering
\includegraphics[width=0.82\textwidth]{fig_central.pdf}
\caption{Productively equivalent compositions with different information feasibility.
\textbf{(a)} Along one certified productive-nullspace direction ($\|Ad\|,\|Bd\|,\|G_Id\|
<10^{-15}$, $\dim\Emanifold=59$), the productive value and the served aggregate are
invariant ($\max_\alpha|V(z(\alpha))-V^{\star}|,\max_\alpha\|Bz(\alpha)-y^{\star}\|<10^{-8}$)
while $\lamtwo(\Lap(z(\alpha)))$ crosses $\sigmastar$. \textbf{(b,c)} A comparative-advantage
role exchange between a matched long/short pair of \emph{equal} lifting capacity, from the
pipeline $z^{\star}\!\to\!$ rounding $\to$ pose $\to\Lgeo^{\pi}(q)$: both use the same robots
and active-role count and share $Bz=y^{\star}$, $V^{\star}$, and the same wrench. (b)~long
robot lifts, short relays (short range, no bridge): $\lamtwo(\Lgeo)\approx0$. (c)~short lifts,
long relays: $\lamtwo(\Lgeo)=0.83\ge\sigmastar$.}
\label{fig:central}
\end{figure*}

\paragraph*{E1 --- Fiber neutrality, certified (Fig.~\ref{fig:central}a)}
At an interior productive optimum $\mathcal N_p=\ker A\cap\ker B$ has dimension
$72-13=59$; moving along it holds $V$ and $Bz$ at machine precision (residuals $<10^{-8}$)
while $\lamtwo$ crosses $\sigmastar$, so the connectivity-feasible subset lies strictly
inside the optimal fiber.

\paragraph*{E2 --- Comparative-advantage exchange (Fig.~\ref{fig:central}b,c)}
A matched long/short pair of equal lifting capacity swaps roles: in the productively
optimal but information-infeasible composition the long-range robot lifts and the short
one relays (its short range bridges nothing, $\lamtwo(\Lgeo)\approx0$); WISE has the short
robot lift and the long robot relay, bridging the clusters ($\lamtwo(\Lgeo)=0.83$). Both
use the same robots and active-role count and share $Bz=y^{\star}$, $V^{\star}$, and zero
wrench residual; only connectivity differs.

\paragraph*{E3 --- Lexicographic vs.\ scalarized selection}
On free-regime seeds ($\LamE\ge\sigmastar$), the scalarised $V_\varepsilon=V+\varepsilon
\lamtwo$ trades productive value for connectivity: as $\varepsilon$ grows from $10^{-3}$ to
$1$ the productive loss rises from $0$ to $0.049$ and the aggregate drift from $0$ to
$0.307$, both under the Tikhonov envelopes, whereas the lexicographic selector holds both
at zero.

\paragraph*{E4 --- Integer recovery (Rem.~\ref{rem:integer})}
Over $30$ seeds a single randomized-rounding draw re-certifies $66\%$ of the fluid optima,
best-of-$30$ all $30$ (argmax $67\%$), with a relative welfare gap
$V_{\mathrm{relax}}-V_{\mathrm{int}}$ of $+1.8\%$ (mean, $+4.5\%$ worst); all robots are
integer.

\paragraph*{E5 --- Method comparison (Table~\ref{tab:ablation})}
Single-criterion baselines rarely clear both tests. The natural strong comparator, the hard
connectivity-constrained optimum $\max_{z\in X_{\mathrm f}}V(z)$ s.t.\ $\lamtwo\ge\sigmastar$,
reaches $V^{\star}$ and certifies every seed in this free regime but only \emph{satisfies}
the requirement (margin $+0.15$), whereas WISE maximizes the margin among productive optima
($+0.99$)---the value of the lexicographic order. The decentralized flow (WISE-PD, $50\%$)
and centralized multistart ($67\%$) attain less than the certificate. Both SDPs solve in
$30$--$60$\,ms at $N=12$.

\begin{table}[b]\centering
\caption{Comparison on $30$ paired seeds ($N=12$, $\nu=0.4$, $\tau_d=6.5$). Productive gap
$V^{\star}-V$, relative wrench deficit, and margin $\lamtwo-\sigmastar$ are means; success
(wrench-feasible \emph{and} $\lamtwo\ge\sigmastar$) carries a Wilson $95\%$ CI (proportion
only). Hard-connectivity and WISE-SDP both reach $V^{\star}$, but WISE attains the larger
spectral margin. The last block is a \emph{relaxed existence} certificate, not an attained
integer controller; integer re-certification is reported separately (E4).}
\label{tab:ablation}
\small
\input{figures/ablation_table.tex}
\end{table}

\paragraph*{E6 --- Closed-loop transport (Fig.~\ref{fig:transport})}
Under the WISE composition the load pose error stays within a bounded tracking band
$\varepsilon_q$ under the moving reference, the normalized wrench residual stays near zero,
and $\lamtwo(\Lgeo^{\pi}(q(t)))$ holds above $\sigmadyn=0.2$ (min $0.58$; here $\sigmadyn=
\vt_1\vt_2/(c\,\mF)$ for representative gains) with the distributed estimate
$\widehat\lamtwo$ tracking within $0.011$ (the margin of Lemma~\ref{lem:bridge}).

\begin{figure}[t]\centering
\includegraphics[width=0.98\columnwidth]{fig_transport.pdf}
\caption{Closed-loop transport under the WISE composition. (a) Load pose error; (b)
normalized undeliverable wrench $r_w/\|w^{\mathrm{dem}}\|$; (c) geometric connectivity
$\lamtwo(\Lgeo^{\pi})$, its affine surrogate $\lamtwo(\Lap)$, and the distributed estimate
$\widehat\lamtwo$, all above $\sigmadyn$.}
\label{fig:transport}
\end{figure}
