\section{Simulation Study}
\label{sec:exp}

We validate the main results on heterogeneous teams ($N=12$ unless noted) in a
two-region world: a rigid load in the right cluster demands a planar wrench, and
short links connect robots only within a cluster, so the gap is bridged only when a
long-range type relays. The strategic layer runs~\eqref{eq:pd}; the physical layer
integrates a rigid load and second-order unicycles whose lifting force is confined to
the heading-aligned elliptical set, so wrench realizability depends on pose.
Connectivity is measured on the geometric Laplacian $\Lgeo(q)$ of the realized poses
while the strategic layer uses the surrogate $\Lap(z)$; the maximum observed mismatch
$\|\Lgeo(q)-\Lap(z)\|_2$ stays within the Lipschitz bound $C_L\rho$ that sizes
$\varepsilon_L$, so Weyl's bound~\eqref{eq:weyl} holds on every sampled state
(Lemma~\ref{lem:bridge}). The primary metric is the \emph{certified service rate}: a
load counts as served only when it is wrench-feasible \emph{and}
$\lamtwo-\sigmastar\ge0$. Every instance is seeded and reproduces exactly.

\begin{figure*}[t]\centering
\resizebox{\textwidth}{!}{\input{sections/fig_concept}}
\caption{The whole problem in one picture. \textbf{(1)}~A distributed
equilibrium-seeker computes the assignment $z$ from aggregates estimated over the
communication graph---but that graph is $\Lap(z)$, formed by $z$ itself, so the
decision builds the graph its own computation needs. \textbf{(2)}~The productive
optimum fixes the delivered wrench $Bz=y^{\star}$ but not the composition: a whole
fiber $\Emanifold$ attains the same value $V^{\star}$ while its induced connectivity
$\lamtwo$ varies, crossing the requirement $\sigmastar$ (inset). \textbf{(3)}~On that
fiber, one composition severs the graph ($\lamtwo<\sigmastar$: the estimator that
produced it goes blind), while a WISE composition diverts one long-range robot to
relay ($\lamtwo\ge\sigmastar$) and both executes \emph{and} computes itself.}
\label{fig:scene}
\end{figure*}

\paragraph*{E1 --- Convergence and self-defeat (Thm.~\ref{thm:stability})}
The decentralized realization~\eqref{eq:pd} drives $\lamtwo(\Lap(z))$ above
$\sigmastar$ and the wrench residual to zero together, with the connectivity price
$\pi$ settling on long-range relays. Comparing two fiber compositions traces the
boundary of Theorem~\ref{thm:stability}: when a long-range robot relays the estimation
error $\|e(t)\|$ decays, and when it is withdrawn ($\lamtwo<\sigmastar$) it
grows---the subsystem defeats its own estimator, with the sign change at
$c\,\mF\,\lamtwo=\vt^2$ (a dedicated sweep is built in the extended study).

\paragraph*{E3 --- Regime phase diagram (Fig.~\ref{fig:phase}, Thm.~\ref{thm:trichotomy})}
Sweeping the long-range fraction $\nu$ and torque demand $\tau_d$ ($n=12$
seeds/cell), the empirical certification rate collapses in the two regimes the
trichotomy predicts: at $\nu\!\to\!0$ no composition reaches $\LamE\ge\sigmastar$
(impossible/costly), and at high $\tau_d$ the fleet is wrench-limited.

\begin{figure}[t]\centering
\includegraphics[width=0.82\columnwidth]{fig3_phase.pdf}
\caption{Empirical certified-service-rate phase diagram over $(\nu,\tau_d)$
($n=12$ seeds/cell). The dashed line is the \emph{empirical} $50\%$ contour, not
the theoretical $\LamE=\sigmastar$ boundary. The trichotomy attributes the two
failure edges to existence ($\nu\!\to\!0$) and wrench feasibility (high $\tau_d$).}
\label{fig:phase}
\end{figure}

\paragraph*{E4 --- Constraint ablation (Table~\ref{tab:ablation}, Fig.~\ref{fig:roles}a)}
On paired seeds, enforcing either requirement alone fails: \texttt{scalar\_capacity}
and \texttt{wrench\_only} build valid coalitions that fragment the network,
\texttt{connectivity\_only} keeps the network but cannot lift, and only WISE
certifies both. WISE and the centralized oracle agree on this operating point,
which is a numerical sanity check (both solve the same relaxed program), not a
proof of distributed optimality. Table~\ref{tab:ablation} reports the run count and
$95\%$ paired-bootstrap intervals.

\begin{table}[t]\centering
\caption{Constraint ablation on $n=16$ paired seeds ($N=12$, $\nu=0.5$,
$\tau_d=3$; mean over seeds). Arrows mark the preferred direction; WISE integer
recovery reaches $100\%$ under randomised rounding.}
\label{tab:ablation}
\small
\input{figures/ablation_table.tex}
\end{table}

\paragraph*{E5 --- Emergent roles (Fig.~\ref{fig:roles}b, Prop.~\ref{prop:cycle})}
Pooled over seeds, relay commitment is drawn from long-range types, as
Proposition~\ref{prop:cycle} predicts: a neutral long/short exchange leaves $Bz$ fixed
while raising $\lamtwo$, so the selector concentrates relays on long-range types---the
predicted division of labour in the tested instances, not a general theorem.

\begin{figure}[t]\centering
\includegraphics[width=\columnwidth]{fig4_ablation_roles.pdf}
\caption{(a) Certified service rate by method (mean $\pm$ 95\% bootstrap CI):
single-requirement methods fail; WISE matches the oracle. (b) Relay share vs.\
communication range at the WISE solution, pooled over seeds---relays are drawn only
from long-range types.}
\label{fig:roles}
\end{figure}